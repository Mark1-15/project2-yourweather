const sum = require('./script.js');

/* test('adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3);
}); */

test('Fahrenheit = ((temperature in celcius) * (9/5)) +32 ', () => {
    expect(sum(10)).toBe(50);
  }); 